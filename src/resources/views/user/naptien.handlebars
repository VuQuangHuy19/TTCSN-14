<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Thông tin ví</h1>

<section class="wallet-info">
    <p>số dư hiện tại: <span id="current-balance">{{formatPrice wallet.balance}} </span></p>

    {{#if successMessage}}
        <p class="success-message">{{successMessage}}</p>
    {{/if}}
</section>

<form class="recharge-form" action="/pro/naptien" method="POST">
    <label for="amount">Số tiền cần nạp:</label>
    <input type="number" id="amount" name="amount" required min="1">
    <button type="submit">Nạp tiền</button>
</form>
</body>
</html>
<style>
    h1{
        text-align: center;
        margin-top: 150px;
    }
    /* Wallet info styling */
.wallet-info {
    background: #fff;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    padding: 20px;
    width: 300px;
    margin: 20px auto;
    text-align: center;
}

.wallet-info p {
    font-size: 1.2rem;
    margin: 10px 0;
}

.wallet-info .success-message {
    color: #28a745;
    font-weight: bold;
}

/* Recharge form styling */
.recharge-form {
    background: #fff;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    padding: 20px;
    width: 300px;
    margin: 20px auto;
    margin-bottom: 150px;
}

.recharge-form label {
    display: block;
    font-size: 1rem;
    margin-bottom: 10px;
    text-align: left;
}

.recharge-form input {
    width: 100%;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
    margin-bottom: 20px;
}

.recharge-form button {
    width: 100%;
    padding: 10px;
    border: none;
    background: #007bff;
    color: white;
    font-size: 1rem;
    border-radius: 5px;
    cursor: pointer;
    transition: background 0.3s ease;
}

.recharge-form button:hover {
    background: #0056b3;
}

</style>